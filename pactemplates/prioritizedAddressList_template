/configure mobile-gateway profile list prioritized-ip-address-list "{{per["prioritizedAddressList"]}}" address {{per["addressOnly"]}}
/configure mobile-gateway profile list prioritized-ip-address-list "{{per["prioritizedAddressList"]}}" address {{per["address"]}} port {{per["port"]}}
/configure mobile-gateway profile list prioritized-ip-address-list "{{per["prioritizedAddressList"]}}" address {{per["address"]}} port {{per["port"]}} priority {{per["priority"]}}
