/configure mobile-gateway profile list slice-list "{{per["sliceList"]}}" slice {{per["slice"]}} sst {{per["sst"]}}
/configure mobile-gateway profile list slice-list "{{per["sliceList"]}}" slice {{per["slice"]}} sst {{per["sst"]}} sd {{per["sd"]}}
/configure mobile-gateway profile list slice-list "{{per["sliceList"]}}" slice {{per["slice"]}} slice-instances {{per["sliceInstance"]}}